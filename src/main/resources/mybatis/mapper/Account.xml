<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!-- 인터페이스에 적절한 DAO를 매핑시키기 위해 작성한다고 보면 될 듯. -->
<mapper namespace="com.tobaki.tobaki.dao.mybatis.Ch13AccountDAO">
	<!-- 
		id에는 태그를 실행시킬 메서드 명을 입력해야 한다. 당연히 DAO에 정의되어 있어야 한다. 
		parameterType은 DTO 객체가 와야한다. 물론 mapper-config에서 별칭을 준 DTO를 적어도 된다.
	-->
	<insert id="insert" parameterType="Account">
		
	</insert>
</mapper>